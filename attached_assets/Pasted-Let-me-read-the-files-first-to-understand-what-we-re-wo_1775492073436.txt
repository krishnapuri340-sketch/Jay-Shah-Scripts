Let me read the files first to understand what we’re working with.​​​​​​​​​​​​​​​​

Now I have a very thorough understanding of the app. Let me ask you the 20 questions:​​​​​​​​​​​​​​​​

I now have everything I need from the code + your answers. Here are the 20 answers I’ve collected and what I’ll build the prompt around — let me know if any need correcting before I write it:

From the code analysis:
	1.	Single 3000+ line App.tsx with 60+ useState hooks — serious re-render risk
	2.	Blurred image backgrounds on every card (lb-card, countdown, match-card) — GPU expensive on mobile
	3.	Canvas-based share functions (~500 lines each) — blocking the main thread
	4.	Polling intervals recreated on every render inside useEffect with isAnyMatchLive dependency
	5.	Admin section styled entirely inline, dark-only, no theme awareness
	6.	Predictions buried inside each individual match card — hard to find
	7.	History tab has great data but minimal visual design (trophy cabinet is just bars)
	8.	Stats tab has a tab within a tab (All IPL / Fantasy / Predictions) which competes with the main nav
From your answers:
9. Performance: Both — split into proper components AND fix inefficiencies
10. Design vibe: Premium sports app (ESPN/Sofascore aesthetic)
11. Admin light mode: Toggle in the settings/gear menu
12. Information density: Rich but well-organised
13. Countdown card: Replace it with a visual matchup card
14. Match cards: Needs a live score visual (mini scorecard on the card itself)
15. Performance pain point: All of these (speed, polling, points sync)
16. New feature excitement: A match simulation / score predictor
17. App-opener feature: A “what if” captain selector
18. Live experience: Add richer scorecard visuals (worm chart)
19. Animations: Only on key moments (big milestones)
20. Player form: Add a trend arrow (up/down/flat) per player
A few more quick ones before I write the prompt:​​​​​​​​​​​​​​​​

Now I have everything. Let me write the prompt.

Your Replit AI Prompt
Here it is — structured, precise, and grounded in the actual codebase:

IPL Fantasy App 2026 — Replit AI Prompt

CONTEXT
This is a private IPL 2026 fantasy cricket tracker for 4 friends (teams: Rajveer Puri / Mombasa K / Mumbai Ma / PonyGoat). The frontend is a single-file React/TypeScript app (fantasy-cricket/src/App.tsx) with an Express API server (api-server/src/). The app is mobile-first, PWA-capable, with a dark cinematic theme. The current App.tsx is ~3000 lines with 60+ useState hooks in a single component.

SECTION 1 — PERFORMANCE & CODE FIXES
1.1 — Component splitting (critical)
Break App.tsx into proper sub-components. Each tab should be its own file:
	∙	components/TabLeaderboard.tsx
	∙	components/TabTeams.tsx
	∙	components/TabFixtures.tsx
	∙	components/TabStats.tsx
	∙	components/TabHistory.tsx
	∙	components/AdminPanel.tsx
	∙	components/LoginScreen.tsx (already partially isolated)
Wrap each tab component in React.memo. Move shared state (playerPoints, liveMatches, predictions, standings, playerMatchPoints) into a useAppData custom hook. This prevents full-tree re-renders when a single state value changes.
1.2 — Blurred background GPU cost
Every leaderboard card, countdown card, and match card uses filter: blur(8px) brightness(...) on an absolutely-positioned <div> with a background image. On lower-end phones this hammers GPU. Fix: use will-change: transform on the blur divs (already partially done), and lazy-load background images — only load lb-bg-*.jpeg for the team card currently in view (use IntersectionObserver or loading="lazy" on a real <img>). Consider converting the blurred-background pattern to a single backdrop-filter on a glass overlay instead of per-card blurred image divs.
1.3 — Polling interval leak
The polling useEffect has [isAnyMatchLive, currentUser] as deps, which means every render where isAnyMatchLive flips, all 5 intervals are torn down and recreated. Extract fetchLive, fetchPoints, fetchStandings, fetchStats, fetchPredictions into stable callbacks using useCallback with appropriate deps, so the interval effect doesn’t needlessly recreate.
1.4 — Canvas share functions
The three canvas-based share functions (shareLeaderboard, shareTeams, shareMatchCard) are defined inline and recreated every render. Move them to a separate utils/share.ts file and call them via refs or stable callbacks. They also load images synchronously via await new Promise — ensure these are properly wrapped with try/catch and abort gracefully if the user cancels before the image loads.
1.5 — API security (minor)
The admin API routes (/api/ipl/points/reset, /api/ipl/scorecard/prefetch-s3, /api/ipl/stats/refresh) check X-Owner-Id: rajveer as the sole auth mechanism. This is fine for a private app, but Replit should add a simple rate limiter (e.g. max 10 admin POST requests per minute per IP) to prevent accidental hammering.
1.6 — localStorage key collision
loadPins(), savePins(), PRED_CACHE_KEY, ipl-current-user all use raw localStorage with string keys and no version prefix. Add a v2: prefix to all keys so future schema changes don’t silently corrupt stored data.

SECTION 2 — DESIGN IMPROVEMENTS
The overall aesthetic should move toward a premium sports app feel (think ESPN / Sofascore) — rich but well-organised, high contrast, big impactful numbers, clean typography. Keep the dark cinematic base but reduce visual noise. The gold (#d4a843) and team colours should pop against cleaner backgrounds.
2.1 — Leaderboard tab
Replace the current countdown card (blurred stadium background + timer + intel + picks) with a visual matchup card:
	∙	Two large team logos facing each other with a VS in the centre
	∙	Countdown timer below in a clean mono font
	∙	A single line: venue name
	∙	Slim picks row (4 owner names + their pick icon, no intel by default)
	∙	The match intel (H2H, predicted total, algo pick) goes into a collapsible drawer that defaults to closed
The leaderboard cards themselves should get a rank number treatment improvement: make the ghost rank number (1, 2, etc.) significantly larger (like Sofascore’s ghost number style — faint, huge, sits behind the content). Add a subtle gap-to-leader indicator below the points for positions 2–4 (−12 pts in red). Remove redundant sub-labels — the team name and owner name don’t both need · separators; use a clear visual hierarchy instead.
Add a prediction summary strip on the home tab: a compact 4-avatar row showing each owner’s pick for the next match, surfaced directly on the leaderboard page — not buried in the match card. Tap it to expand inline.
2.2 — Teams tab
The avatar selector row at the top is good but the active state is unclear on smaller phones. Make the selected team’s avatar ring glow with a stronger pulse (use CSS animation, not just a border).
Add a trend arrow (↑ flat ↓) per player in the player list, calculated from their last 3 matches compared to their season average. Place it beside the sparkline.
The segmented pill (Playing XI / Bench / Match Pts) works well but the “Match Pts” label is unclear to non-technical users — rename to “By Match”.
The player breakdown accordion (match-by-match scoring lines) is excellent — keep it but add a total points trajectory chart at the top of the “By Match” view: a simple SVG line chart showing cumulative fantasy points across all matches for this team. Use the existing matchHistory data already computed in the app.
2.3 — Matches tab
Match cards should show a live score visual directly on the card face (without needing to expand the scorecard): for live and completed matches, show a compact two-line score summary styled as a mini scoreboard — big innings score, small overs bracket, team name in team colour. This replaces the current text-only score lines.
The Points Table (standings) at the top of the Fixtures tab is excellent — no changes needed. But the team filter chips below it should automatically activate when you tap a team row in the standings (currently tapping a standings row toggles filter but the UX isn’t signposted).
The prediction section inside each match card needs to be more visually distinct — currently it’s just a row of grey text. Make the editable pick buttons bigger and more tactile. When a pick is confirmed, show a brief checkmark animation on the button (not just a dot).
2.4 — Stats tab
The three-segment pill (All IPL / Fantasy / Predictions) at the top of Stats is the right idea, but Predictions being here feels misplaced since it’s really a social/game feature. Instead:
	∙	Keep All IPL and Fantasy segments in Stats as stat leaderboards
	∙	Move Predictions into its own surfaced section: add a Predictions Summary card on the home (Leaderboard) tab. Show a compact scoreboard of correct picks per owner (e.g. Raj: 8 ✓, Rahul: 6 ✓, Smeet: 5 ✓, Deb: 7 ✓). Tapping expands it. The full prediction table remains accessible in Stats for the commissioner.
2.5 — History tab
The history tab has great data (trophy cabinet, season detail, top 10 bat/bowl). Design improvements:
	∙	The trophy cabinet should use actual team logo images (already defined in TEAM_LOGO_CDN) instead of the badge abbreviation circles — much more visual impact
	∙	The season detail hero card needs a final score shown — e.g. “Final: CSK vs SRH” — this data isn’t currently stored, add it to IPL_HISTORY constants
	∙	The all-seasons list cards should show the final score line in addition to champion/runner-up
2.6 — Admin section + light mode
Add a light mode toggle accessible from the settings/gear dropdown (currently used for Logout + League Control Centre). When light mode is active, only the Admin panel renders in light theme — all other tabs remain dark. Save the preference to localStorage.
Admin light mode design: clean data dashboard feel (Notion/Linear aesthetic). Use:
	∙	#f8f9fa page background
	∙	#ffffff card backgrounds with 1px solid #e5e7eb borders
	∙	#111827 primary text, #6b7280 secondary
	∙	Status colours remain vivid (green/red/amber) for readability
	∙	The gold accent becomes #b8860b (darker gold for readability on light)
	∙	All the inline dark colour strings (rgba(15,21,32,0.9), rgba(255,255,255,0.06) etc.) must be replaced with CSS variables that flip based on a .admin-light class on the admin wrapper div
All admin section cards (Auto-Points Engine, Player Points Breakdown, PIN Management, buttons) need their background/border/text colours updated to use these CSS variables.
2.7 — Typography consistency
The app mixes 'Oswald', 'Bebas Neue', font-family: inherit and inline -apple-system references. Standardise:
	∙	Numbers (scores, points, ranks): 'Oswald', sans-serif — already used in most places, make it universal
	∙	All body/UI text: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
	∙	Remove 'Bebas Neue' references (only used in admin player pts) and replace with Oswald for consistency

SECTION 3 — NEW FEATURE: “WHAT IF” TAB
Add a 6th tab to the nav bar labelled “What If” with a dice/sparkle icon. This is an interactive fantasy lab accessible to all 4 users.
Feature: Captain Swapper
Each user can view their own team and ask: “What if I’d picked a different captain for a past match?”
UI flow:
	1.	User opens “What If” tab — sees their team (same avatar selector row as Teams tab)
	2.	A list of past matches is shown (M1 → current), each showing their team’s actual points that match
	3.	Tap any match → expand to show all 11 players with their actual points for that match
	4.	A “Swap captain” toggle lets them set any player as hypothetical C (×2) or VC (×1.5)
	5.	The projected total recalculates instantly with the new captain multiplier
	6.	Show a delta card: e.g. “+24 pts” (green) or “−8 pts” (red) vs actual
	7.	A cumulative “Best possible season” score is shown at the top — auto-calculated by finding the optimal captain per match across all matches (a fun stat)
Feature: Match Score Predictor
Below the captain swapper, a dedicated pre-match predictor card appears for the next upcoming match:
	∙	Shows the two teams, their batting/bowling ratings (already computed as TEAM_BATTING / TEAM_BOWLING)
	∙	Shows the venue average score (already in VENUE_AVG)
	∙	Shows the H2H record (already computed via getH2H)
	∙	Shows the algo pick (already computed via predictNextMatch)
	∙	Adds a “Your gut pick” CTA — the user taps their predicted winner. This saves to the same predictions system so it’s reflected on the Leaderboard predictions strip
	∙	A “Predicted first innings total” slider — the user drags to their predicted score (range: venue avg −30 to +30). After the match, if they were within ±10, they get a visual “Nice call!” badge on the next match card
The predictor card uses the existing venue/H2H/prediction data — no new API calls needed.

IMPLEMENTATION NOTES FOR REPLIT
	∙	All new components should be TypeScript with proper typing — do not use any except where the existing API response types require it
	∙	Do not change the API server routes or data models — the frontend changes only touch fantasy-cricket/src/
	∙	The teams.ts, constants.ts, utils.ts files should not be modified unless adding new constants for the What If feature
	∙	CSS variables for light/dark admin should be scoped — don’t accidentally affect the main dark theme
	∙	The new “What If” tab should be added to the SWIPEABLE_TABS array in constants.ts so swipe navigation works
	∙	Ensure the nav bar still works cleanly with 6 tabs on small screens (consider hiding labels for 2 least-used tabs, or reducing font size to 0.56rem)
	∙	The “Best possible season” calculation should be useMemo-cached — it iterates all matches × all players

That’s the full prompt. A few things to do before you paste it into Replit:
	1.	Answer Q13 and Q14 from my buttons above — I noticed your answers weren’t captured for “New feature nav location”, “Predictions fix” and “Captain selector user access.” If those differed from what I assumed (6th tab, summary card on home, all 4 users), let me know and I’ll adjust the prompt.
	2.	The prompt as written is Phase 1 + Phase 2 combined — if Replit starts choking, split at the SECTION 3 header and do the What If tab separately.
	3.	The light mode admin is the trickiest part technically — tell Replit explicitly not to use Tailwind classes for this (since you’re not running a Tailwind compiler) and to use scoped CSS variables only.​​​​​​​​​​​​​​​​